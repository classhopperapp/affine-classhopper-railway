# PostgreSQL şifresi (Railway'de otomatik atanır, elle değiştirmek gerekmez)
POSTGRES_PASSWORD=your-secure-password

# AFFiNE ortamı
NODE_ENV=production

# Worker sayısı (isteğe bağlı)
AFFINE_PUB_WORKER_COUNT=1
